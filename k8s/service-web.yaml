apiVersion: v1
kind: Service
metadata:
  name: app-service
spec:
  selector:
    app: web-app
  ports:
    - protocol: TCP
      port: 80         # Port du service
      targetPort: 3000  # Port de ton app Node
      nodePort: 30001  # Port que tu utiliseras sur ton navigateur
  type: NodePort